exit
cards[0].id === params[:card_id]
cards[0].id == params[:card_id]
cards[0]
cards[1]
params[:card_id]
cards.select{|card| card.id== params[:card_id]}
cards = user_cards.map{|card| card.card}
cards = user_cards.map{|card| user_cards.card}
user_cards[1].card.id
usercards[1].card.id
user_cards
card
exit
id
user_cardz.each{|card| card.card.id == params[:card_id] ? id = card.card.id : nil }
user_cardz.each{|card| card.card.id == params[:card_id] ? id = card.card.id :nil }
user_cardz.each{|card| if card.card.id == params[:card_id] id = card.card.id }
id = 0
user_cardz.each{|card| if card.card.id == params[:card_id] $card = card.card }
user_cardz.each{|card| if card.card.id == params[:card_id] $card = card.card end }
user_cardz.each{|card| if card.card.id == params[:card_id] {$card = card.card}}
user_cardz.each{|card| if card.card.id == params[:card_id] {$card = card.card}
card = ""
params[:card_id]
user_cardz.select{|card| card.card.id == params[:card_id]}
user_cardz = user.user_cards
user.user_cards.each{|card| pp card.card_id}
user.user_cards.each{|card| pp card}
user.user_cards.each{|card| pp card.id}
user.user_cards.each{|card| pp card.card.id}
user.user_cards.first.card.id
user.user_cards.first.card
user_cards.first.card
user.user_cards
user.user_cards.each{|card| p card.card.id}
user.user_cards.each{|card| p uCard.card.id}
card = user.user_cards.select{|uCard| uCard.card.id == params[:card_id]}
card
exit
c
next
user.user_cards
exit
card = User.cards.find(params[:card_id])
user.cards
user
user = User.find(params[:user_id])
params
